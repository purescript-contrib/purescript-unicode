{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build && pulp test && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "jscs": "^1.13.1",
    "jshint": "^2.8.0",
    "pulp": "^4.0.2",
    "rimraf": "^2.4.1"
  }
}
