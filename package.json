{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build && pulp test && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "bower": "^1.7.2",
    "pulp": "^7.0.0",
    "rimraf": "^2.5.0"
  }
}
